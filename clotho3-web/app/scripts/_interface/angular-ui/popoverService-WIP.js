angular.module('clotho.interface').service('popoverService', [ '$compile', '$timeout', '$parse', '$window', '$tooltip',  function ( $compile, $timeout, $parse, $window, $tooltip ) {

	//todo - hack to show by parameter

	//note - may make more sense to make a decorator?

	//given an object with popover content and elements to placement

	//append a popover to the DOM

	//add custom options

	//step through each popover in passed object

	//or show them all at once


}]);